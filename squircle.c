#include <stdio.h>

struct Squircle
{
    int a, b; // a and b are the x and y coordinates of the center of the ellipse
    int n; // positive number
    int r;
    char point_sym;
};

void squircle(struct Squircle sq)
{
    
}
int main(int argc, char* argv[])
{
    
    return 0;
}
